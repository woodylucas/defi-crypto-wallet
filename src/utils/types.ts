// types.ts
export type PortfolioData = {
  balance: string;
  change: string;
};

export type TrendingCurrency = {
  id: string;
  name: string;
  symbol: string;
  price: string;
  change: string;
  image: string;
};
